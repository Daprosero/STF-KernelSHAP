[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "stf-kernelshap"
version = "0.1.0"
description = "STF-KernelSHAP EEG classification and interpretability utilities."
requires-python = ">=3.12"

[tool.setuptools.packages.find]
where = ["src"]
